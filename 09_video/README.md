# 09_video — Epic 9 working directory

Final deliverables for the take-home video (PT-BR, ≤ 5 minutos). Populated by Epic 9.

**Demo URL for Act 3** (live as of Epic 8.5 / D-007, 2026-05-12): `https://revelatio-demo.streamlit.app` — open the URL in the browser instead of running `streamlit run` locally. The Google Sheets write-back button is the strongest visual *closer* (panel can click it after the video to confirm it ran end-to-end).

Expected contents after Epic 9 completes:

| File | Purpose |
|---|---|
| `script.md` | PT-BR script with timing budgets per act + the demo cue sheet |
| `shot_list.md` | What's on screen at each second (Streamlit, relatório PDF page numbers, terminal, etc.) |
| `recording_notes.md` | Take notes, retries, recording tool used, total raw length, final length |
| `revelatio_demo.mp4` (or Loom link) | Final video file or shareable Loom URL |

**To start Epic 9**, open a fresh Claude Code session in this repo and paste the prompt from `../09_kickoff_prompt.md`.
