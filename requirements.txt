# Runtime dependencies for Streamlit Community Cloud.
# Pinned to versions verified in .venv during Epic 8.5 development.
# See DEPLOY.md for the hosted-deploy runbook.

streamlit==1.57.0
anthropic==0.100.0
python-dotenv==1.2.2
azure-ai-documentintelligence==1.0.2
azure-core==1.41.0
gspread==6.2.1
google-auth==2.52.0
